---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: backups.task.a-cool-name.io
spec:
  group: task.a-cool-name.io
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Backup is the Schema for the backups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines the desired state of Backup
            properties:
              name:
                description: Name of the backup
                type: string
              schedule:
                description: "Schedule of the backup ex: \"0 0 * * *\", run every
                  day at midnight \n required if Type is set to \"cronjob\", see https://crontab.guru/
                  for more information"
                type: string
              type:
                description: Type of backup ("job", "cronjob")
                type: string
            required:
            - name
            - type
            type: object
          status:
            description: BackupStatus defines the observed state of Backup
            properties:
              conditions:
                description: Conditions of the backup
                items:
                  properties:
                    lastUpdateTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Message about the condition for a better understanding
                      type: string
                    reason:
                      description: Reason for the backup
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type (Ready, Failed)
                      type: string
                  type: object
                type: array
              phase:
                description: Phase of the backup
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
